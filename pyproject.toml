[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "deribit-options-flow"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["fetcher", "processor", "signal"]
